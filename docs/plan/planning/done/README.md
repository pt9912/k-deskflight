# Abgeschlossene Pläne

Dieses Verzeichnis sammelt Closure-Notizen zu abgeschlossenen
Meilensteinen und Plänen.

Eine Closure-Notiz fasst zusammen:

- was wurde geliefert (Code, Specs, ADRs),
- welche Lastenheft-Kennungen sind damit umgesetzt,
- was wurde explizit nicht erledigt und wandert weiter (`../open/`,
  `../next/` oder Folge-Meilenstein),
- Verweis auf Tag/Release im CHANGELOG (sobald vorhanden).

---

## Bestand

| Datei | Geschlossen | Lieferziel |
| ----- | ----------- | ---------- |
| [`slice-M1-repo-skeleton.md`](slice-M1-repo-skeleton.md) | 2026-05-17 | Go-Modul-Skeleton, Verzeichnis-Layout, Multi-Stage `Dockerfile`, `Makefile` (Docker-only), `.golangci.yml` (5 Default + 24 SOLID Linter), Doc-Refs- und Coverage-Gate-Skripte, GitHub-Actions-CI mit `gates` + `security-gates` parallel. Alle lokal verifizierbaren Abnahmekriterien aus §7 grün. |
| [`slice-M2-crd-controller-skeleton.md`](slice-M2-crd-controller-skeleton.md) | 2026-05-17 | CRD `OpenDeskPreflightCheck` v1alpha1 (kubebuilder-Marker, controller-gen-Pipeline mit `tools`-Stage), Reconciler-Skelett (Pending→Running→Passed mit M3-Fix), controller-runtime v0.24.1, depguard nach AR-005 scharf, Generated-Drift-Gate aktiv, deploy/manifests + sample CR. **Alle §7-Items inkl. LH-AK-001/-002/-003/-004/-011 attestiert** (lokal via Tests, real via Cluster-Smoke nach ADR 0013). |
| [`slice-M3-kubernetes-version-check.md`](slice-M3-kubernetes-version-check.md) | 2026-05-17 | Erste echte Check-Implementierung: `KubernetesVersionCheck` vergleicht `discovery.ServerVersion()` mit `spec.checks.kubernetesVersion.min`. AR-012 Check-Interface, AR-013 Registry (Port + Map-Adapter), AR-014 Aggregator (Severity→Phase + Conditions-Dedupe/Sort). Reconciler durchläuft minimal-sequenziellen AR-009-Pfad (Phasen 1+2+3+4+5+6 ohne Worker-Pool). Coverage 86.1 %. **LH-AK-005 inkl. realer Cluster-Verifikation** über `cluster-smoke`-Workflow (ADR 0013). |
| [`slice-M4-cluster-state-checks.md`](slice-M4-cluster-state-checks.md) | 2026-05-18 | Vier weitere Checks (`StorageClass`, `IngressClass`, `CertManager`, `ClusterResources`) schließen das MVP-Pflichtset (`LH-PRI-001`). Interface-Segregation pro Discovery-Domain (vier neue `port.*Discovery`-Interfaces); Adapter-Bündel über `NewClusterClients`. cert-manager-Severity bewusst `warning` (slice §9, Plichtmessage-Substring „external TLS termination" fixiert). Coverage strikt 90 % (von 76.9 % auf 95.8 % gehoben, `Makefile.THRESHOLD` von 0 auf 90 — slice-M4 zieht `ADR 0012 §2.5`-Wert vor; M6 erbt). Cluster-Smoke nutzt Minimal-Stubs unter `hack/cluster-smoke/` statt voller Upstream-Manifeste. **`LH-AK-006..009` real attestiert** im `cluster-smoke`-Workflow. |
| [`slice-M5-rbac-self-check-robustness.md`](slice-M5-rbac-self-check-robustness.md) | 2026-05-18 | SelfSubjectAccessReview pro Check (`AR-018`), Panic-Härtung (Per-Check-Recover umschließt SAR-Loop + Run, plus Reconciler-Outer-Recover mit best-effort Status-Write), Per-Check-Timeout 30 s mit `context.WithTimeoutCause` für Cause-aware Diskriminierung (Timeout/ReconcileTimeout/ReconcileCanceled), Secret-Sanitize-Hooks (`SanitizeMessage` + `SanitizeAttrs` + `LogResult`-Wrapper) als Pflicht-Pattern. Drei-Outcome-SAR-Kontrakt (RBACInsufficient vs. RBACCheckFailed) verhindert Verschleierung von Auth-Subsystem-Ausfällen. RBAC-Konsistenz-Tests via `go/parser` decken beide Drift-Richtungen automatisch. Coverage 94.7 %. **`LH-AK-010/-012/-015/-016` (`LH-F-024`, `LH-NF-005`, `LH-SEC-001/002/005`)** attestiert; cluster-smoke real grün mit allen fünf Conditions=True. |
| [`slice-M6-metrics-tests-doku.md`](slice-M6-metrics-tests-doku.md) | 2026-05-19 | `/metrics`-Endpoint via dediziertem `Service`-Objekt exponiert + Pattern-Asset-ClusterRole (Auth-Filter v0.2-Übergabe an ADR-Folge zu `ADR 0007`). End-zu-End-Attestation via Probe-Pod-Curl gegen Service-DNS-FQDN mit 3-Assert-Block (Format/Inhalt/Sanity). Cluster-Smoke um vier failed-CRs erweitert (serielles Apply + Wait + Co-Failure-Gegenprobe) — `LH-AK-005/-006/-007/-009` jetzt auf passed+failed-Pfaden attestiert; cert-manager-Ausnahme dokumentiert. `Spec.Interval`-Normalisierung mit drei spezifischen Reasons (`IntervalUnparseable`/`-ClampedMin`/`-ClampedMax`, AR-010-konform; `mergeIntervalWarning`+`escalatePhase` schützen Failed/Unknown vor Down-Escalation). Anwender-Doku unter `docs/user/` mit fünf Dateien (README, installation, cr-examples, conditions, troubleshooting) — `LH-AK-013` attestiert. Architektur-Punkte `AR-OP-005` (Namespace-Override-Mechanik), `AR-OP-009` (controller-runtime-RequeueAfter-Pflicht-Pattern) und `AR-OP-010` (Reason-Naming-Konvention `Reason<Feature><State>`) geschlossen. Coverage 92.5 %. Drei Code-Review-Runden + eine Doku-Review-Runde + ein Closure-Review (insgesamt 5 Fixup-Commits). |
| [`slice-M7-release-v0.1.0.md`](slice-M7-release-v0.1.0.md) | 2026-05-20 | MVP-Release-Slice: anwendungsorientierte CR-Vorlagen (`deploy/samples/cluster-readiness-{production,evaluation}.yaml`), `CHANGELOG.md` (Keep-a-Changelog 1.1.0, Englisch), Lastenheft-Sync `LH-PROD-003a`/`-003b` an post-M2-CRD-Feldnamen, Leader-Election scharf (`AR-026`, `--leader-elect=true` plus Single-Pod-Topologie-Guard mit Reason-Marker `SinglePodTopologyMismatch`), `make image-build VER=`/`image-publish-{dry-run,guard,}`/`image-scan`/`security-gates VER=`/`release-guard` als m-trace-adaptiertes Release-Tooling mit Approval-ENVs `K_DESKFLIGHT_*`, Trivy-Pattern mit expires-checked Vulnignore (Erst-Run `Total: 0` über CRITICAL/HIGH/MEDIUM gegen `distroless/static-debian12:nonroot`), 11 Failure-Path-Tests im `release-guard-test`, ADR 0011 §2.4 zweistufiges Grandfathering + DCO-Bot-Aktivierung als Bruchkante (88 Pre-Bot-Commits gezählt). Operativ: DCO-Bot installiert + PR #1 grün, Repository public, GHSA + Dependabot scharf, Repository-Topics (9). `LH-AK-013` (Doku + Sample-Manifeste) und `LH-AK-014` (Public + GHSA + CHANGELOG) operativ geschlossen; `v0.1.0`-Tag-Erzeugung und GHCR-Image-Publish sind §10.5-Folge-Attest. **Sammel-Closure:** die Roadmap selbst wandert mit dieser Slice-Closure ebenfalls nach `done/`. |
| [`roadmap.md`](roadmap.md) | 2026-05-20 | Sammel-Closure-Notiz für die MVP-Roadmap v0.1. Sieben Slices M1–M7 alle Done; deckt `LH-MVP-002`/`LH-PRI-001` plus `LH-AK-001..-016` ab. Folge-Roadmap für v0.2+ entsteht später unter `in-progress/`. |
| [`changelog-trigger.md`](changelog-trigger.md) | 2026-05-20 | Trigger-Watch für CHANGELOG-Format/-Pflege vor dem ersten Release-Tag (`LH-REL-001`). Mit der M7-Closure als „in M7 entschieden" gelöst (Keep-a-Changelog 1.1.0, Englisch, manuelle Pflege; siehe `slice-M7-release-v0.1.0.md §2.2`). Rename von `changelog.md` → `changelog-trigger.md` beim Move aus `open/`, damit der Pfad in `done/` nicht mit dem Repo-Root-`CHANGELOG.md` kollidiert. |
