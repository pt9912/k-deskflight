---
# Minimal-CR für slice M2: Schema-Defaults greifen (Profile=production,
# spec.checks.kubernetesVersion.min="1.34") — der Reconciler schreibt
# Status.Phase=Passed mit leerer Summary, weil M2 noch keine Check-
# Logik implementiert (Roadmap §3 M2).
apiVersion: k-deskflight.geo-terrain.net/v1alpha1
kind: OpenDeskPreflightCheck
metadata:
  name: smoke
  namespace: default
spec: {}
