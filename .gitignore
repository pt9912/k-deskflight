# =============================================================================
# k-deskflight .gitignore
#
# Operator-Repository in Go mit Docker-only-Workflow (siehe
# docs/plan/planning/done/slice-M1-repo-skeleton.md §2.1). Wir
# halten dieses File bewusst schlank — nur Pfade, die tatsächlich
# entstehen können.
# =============================================================================

# ---------------------------------------------------------------------------
# OS
# ---------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------------
# IDE / editors
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.iml
*.iws
*.ipr
.fleet/
*.swp
*.swo
*~

# ---------------------------------------------------------------------------
# Logs
# ---------------------------------------------------------------------------
*.log
logs/

# ---------------------------------------------------------------------------
# Environment / secrets
# ---------------------------------------------------------------------------
.env
.env.local
.env.*.local
!.env.example

# ---------------------------------------------------------------------------
# Go
# ---------------------------------------------------------------------------
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.prof
coverage.txt
coverage.out
vendor/
bin/
build/

# ---------------------------------------------------------------------------
# Docker
# ---------------------------------------------------------------------------
*.pid

# ---------------------------------------------------------------------------
# Local/temporary data
# ---------------------------------------------------------------------------
tmp/
.tmp/
out/
*.bak

# Docker-Modul-/Build-Cache aus lokalen Mounts (falls jemand außerhalb
# des Containers experimentiert).
.gomodcache/

# ---------------------------------------------------------------------------
# Cluster-Smoke (ADR 0013)
# ---------------------------------------------------------------------------
# scripts/cluster-smoke.sh legt eine kustomize-Overlay-Datei mit
# Image-Substitution kurzzeitig hier ab und räumt sie via trap auf.
# Falls der trap nicht greift, soll der Restbestand nicht committed
# werden.
.cluster-smoke-overlay/

# Attest-Artefakt vom Cluster-Smoke: enthält den CR-Status-Dump (Phase,
# Conditions, Summary). Wird im CI-Workflow als Upload-Artefakt
# hochgeladen, lokal nicht committed.
.cluster-smoke-attest.yaml

# ---------------------------------------------------------------------------
# Security-Gates (kommt in M7 mit Trivy)
# ---------------------------------------------------------------------------
.security/.trivy-cache/
.security/.trivyignore

# ---------------------------------------------------------------------------
# Tooling state
# ---------------------------------------------------------------------------
.claude/
.codex
